# Optional: install WSL 2 for Docker Desktop on Windows
Write-Host "WSL is required for Docker Desktop on Windows." -ForegroundColor Cyan
Write-Host "Run in an elevated PowerShell:" -ForegroundColor Yellow
Write-Host "  wsl --install" -ForegroundColor White
Write-Host "Then restart and open Docker Desktop." -ForegroundColor Gray
Write-Host "Docs: https://docs.docker.com/desktop/setup/install/windows-install/" -ForegroundColor Gray
